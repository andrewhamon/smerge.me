<%= form_for(link) do |f| %>
  <% if link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(link.errors.count, "error") %> prohibited this link from being saved:</h2>

      <ul>
      <% link.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :spotify_playlist_id %>
    <%= f.select :spotify_playlist_id, options_for_select(@playlists) %>
  </div>

  <div class="field">
    <%= f.label :github_repo_id %>
    <%= f.select :github_repo_id, options_for_select(@repos) %>
  </div>

  <div class="field">
    <%= f.label :github_file_path %>
    <%= f.text_field :github_file_path %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
